Index: gums-client/pom.xml
===================================================================
--- gums-client/pom.xml.orig	2012-02-16 16:39:26.000000003 -0600
+++ gums-client/pom.xml	2012-02-17 17:01:50.000000003 -0600
@@ -287,6 +287,7 @@
 					</execution>
 				</executions>
 			</plugin>
+<!-- maven-release-plugin requires maven 2.0.9+
 			 <plugin>
             <artifactId>maven-release-plugin</artifactId>
                 <version>2.1</version>
@@ -294,25 +295,27 @@
                     <tagBase>
                     https://svn.usatlas.bnl.gov/svn/privilege/tags
                     </tagBase>
-                    <!-- 
+                    <!- 
                     <checkModificationExcludes>
                         <checkModificationExclude>${basedir}/src/main/java/org/apache/xml/xml_soap/MapItem.java</checkModificationExclude>
                         <checkModificationExclude>${basedir}/src/main/java/gov/bnl/gums/admin/AdminSoapBindingStub.java</checkModificationExclude>
                         <checkModificationExclude>${basedir}/src/main/java/gov/bnl/gums/admin/GUMSAPIService.java</checkModificationExclude>
                         <checkModificationExclude>${basedir}/src/main/java/gov/bnl/gums/admin/GUMSAPIServiceLocator.java</checkModificationExclude>
                     </checkModificationExcludes>
-                    -->
+                    ->
                 </configuration>
             </plugin>
+-->
 			
-			<plugin>
+        <!--
+commented out by drs - the rpm program is not available on all platforms and this might
+hide other problems. To build an rpm, type mvn rpm:rpm 
+
+However, it can only be run from the normal lifecycle, no via rpm:rpm, so it must stay. 
+            <plugin>
                 <groupId>org.codehaus.mojo</groupId>
                 <artifactId>rpm-maven-plugin</artifactId>
-<!-- commented out by drs - the rpm program is not available on all platforms and this might
-     hide other problems. To build an rpm, type mvn rpm:rpm 
-     
-     However, it can only be run from the normal lifecycle, no via rpm:rpm, so it must stay. 
--->
+
 
                 <executions>
                     <execution>
@@ -378,7 +381,8 @@
                     </mappings>                    
                 </configuration>
             </plugin>
-		</plugins>
+        -->
+        </plugins>
     </build>
 	 
  	<dependencies> 
Index: pom.xml
===================================================================
--- pom.xml.orig	2012-02-16 16:39:26.000000003 -0600
+++ pom.xml	2012-02-17 17:01:50.000000003 -0600
@@ -85,6 +85,7 @@
     </modules>
 
     <build>
+            <!-- commented out because maven-release-plugin 2.2.1 requires maven 2.0.9 and we only have 2.0.7
         <pluginManagement>
             <plugins>
                 <plugin>
@@ -94,6 +95,7 @@
                 </plugin>
             </plugins>
         </pluginManagement>
+             -->
         
         <plugins>
             <plugin>
@@ -106,6 +108,7 @@
                 </configuration>
             </plugin>
               
+            <!-- commented out because maven-release-plugin 2.2.1 requires maven 2.0.9 and we only have 2.0.7
             <plugin>
                 <artifactId>maven-release-plugin</artifactId>
                     <version>2.2.1</version>
@@ -116,6 +119,7 @@
                             <autoversionsubmodules>true</autoversionsubmodules>
                         </configuration>
              </plugin>
+             -->
              <plugin>
                     <groupId>org.apache.maven.plugins</groupId>
                     <artifactId>maven-compiler-plugin</artifactId>
Index: gums-service/pom.xml
===================================================================
--- gums-service/pom.xml.orig	2012-02-16 16:39:26.000000003 -0600
+++ gums-service/pom.xml	2012-02-17 17:02:31.000000003 -0600
@@ -235,6 +235,7 @@
                     </deployer>
                 </configuration>
            </plugin>
+<!--
            <plugin>
             <artifactId>maven-release-plugin</artifactId>
             <version>2.1</version>
@@ -244,6 +245,7 @@
                 </tagBase>
             </configuration>
             </plugin>
+-->
         </plugins>
         <!-- 
        <resources>
