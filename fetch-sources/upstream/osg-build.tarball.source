# tarball created via
# git clone https://github.com/opensciencegrid/osg-build
# cd osg-build
# git archive --format=tar --prefix=osg-build-1.99.5/ v1.99.5 | gzip -9n > osg-build-1.99.5.tar.gz
# We want the sources from osg-build, but not the spec file, which is why we can't use the 'github' type

osg-build/1.99.5/osg-build-1.99.5.tar.gz sha1sum=5e376a168c3d314fc5dd2ca3ab905e602a429ccd
