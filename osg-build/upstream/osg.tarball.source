# To generate the tarball, check out software/osg-build/trunk from the VDT SVN
# and run 'make dist'. Run 'make afsdist' to automatically place it into the
# upstream directory on AFS.
osg-build/1.3.5/osg-build-1.3.5.tar.gz
