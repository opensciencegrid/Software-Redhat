Index: gip-1.3.10/gip/libexec/osg_info_wrapper.py
===================================================================
--- gip-1.3.10.orig/gip/libexec/osg_info_wrapper.py	2013-05-22 15:21:10.000000000 -0500
+++ gip-1.3.10/gip/libexec/osg_info_wrapper.py	2014-02-19 17:47:28.402356208 -0600
@@ -47,10 +47,10 @@
 from gip_ldap import read_ldap, compareDN, LdapData
 import gip_sets as sets
 
-# check if we are root.  If we are, drop privileges to daemon to avoid
-# permissions problems when CEMon tries to run the GIP
+# check if we are root.  If we are, drop privileges to gip to avoid
+# permissions problems when OSG-Info-Services tries to run the GIP
 cp = config()
-gip_user = cp_get(cp, "gip", "gip_user", "tomcat")
+gip_user = cp_get(cp, "gip", "gip_user", "gip")
 start_uid = os.getuid()
 if start_uid == 0:
     # NOTE:  Must set gid first or you will get an "Operation not permitted"
