MyProxy is mostly a pass-through from EPEL. The one change we make is
we disable the %check section in the build since it requires a locally
running service, which we don't have in our build environment.
We used to get it from the Globus repos earlier.
